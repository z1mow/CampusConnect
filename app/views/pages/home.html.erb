<div class="landing-page">
  <div class="hero-section">
    <div class="hero-content">
      <h1>
        <i class="graduation cap icon"></i>
        CampusConnect
      </h1>
      <p class="hero-subtitle">Üniversite topluluklarını keşfet, bağlan, iletişimde kal!</p>
      
      <% unless logged_in? %>
        <div class="cta-buttons">
          <%= link_to new_user_path, class: "ui huge primary button" do %>
            <i class="user plus icon"></i>
            Hemen Katıl
          <% end %>
          <%= link_to login_path, class: "ui huge basic button" do %>
            <i class="sign in icon"></i>
            Giriş Yap
          <% end %>
        </div>
      <% end %>
    </div>
  </div>

  <div class="features-section">
    <h2>Neler Sunuyoruz?</h2>
    <div class="features-grid">
      <div class="feature-card">
        <i class="users icon"></i>
        <h3>Topluluk Grupları</h3>
        <p>İlgi alanlarına göre gruplara katıl, yeni arkadaşlar edin ve etkinlikleri takip et.</p>
      </div>

      <div class="feature-card">
        <i class="comments icon"></i>
        <h3>Gerçek Zamanlı Sohbet</h3>
        <p>Grup üyeleriyle anlık mesajlaş, fikir alışverişinde bulun ve iletişimde kal.</p>
      </div>

      <div class="feature-card">
        <i class="calendar icon"></i>
        <h3>Etkinlik Takibi</h3>
        <p>Topluluğunun düzenlediği etkinliklerden haberdar ol ve katılım sağla.</p>
      </div>

      <div class="feature-card">
        <i class="shield icon"></i>
        <h3>Güvenli Ortam</h3>
        <p>Sadece üniversite mail adresi olan öğrenciler katılabilir, güvenli bir platform.</p>
      </div>
    </div>
  </div>

  <% if logged_in? %>
    <div class="groups-section">
      <h2>Popüler Topluluklar</h2>
      <div class="groups-grid">
        <%= link_to community_groups_path, class: "explore-button" do %>
          <i class="compass icon"></i>
          Tüm Toplulukları Keşfet
        <% end %>
      </div>
    </div>
  <% end %>
</div>

<style>
.landing-page {
  margin-top: -2rem;
}

.hero-section {
  background: linear-gradient(135deg, #00B5AD 0%, #009c95 100%);
  color: white;
  padding: 6rem 2rem;
  text-align: center;
  margin-bottom: 4rem;
  position: relative;
  overflow: hidden;
}

.hero-section::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: url('path_to_pattern.png');
  opacity: 0.1;
}

.hero-content {
  max-width: 800px;
  margin: 0 auto;
  position: relative;
  z-index: 1;
}

.hero-section h1 {
  font-size: 3.5em;
  margin-bottom: 1rem;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 1rem;
}

.hero-subtitle {
  font-size: 1.5em;
  margin-bottom: 2rem;
  opacity: 0.9;
}

.cta-buttons {
  display: flex;
  gap: 1rem;
  justify-content: center;
}

.features-section {
  padding: 4rem 2rem;
  text-align: center;
  max-width: 1200px;
  margin: 0 auto;
}

.features-section h2 {
  font-size: 2.5em;
  color: #2c3e50;
  margin-bottom: 3rem;
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 2rem;
  padding: 0 1rem;
}

.feature-card {
  background: white;
  padding: 2rem;
  border-radius: 12px;
  box-shadow: 0 4px 16px rgba(31, 38, 135, 0.07);
  transition: all 0.3s ease;
}

.feature-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 6px 20px rgba(31, 38, 135, 0.1);
}

.feature-card i {
  font-size: 2.5em;
  color: #00B5AD;
  margin-bottom: 1rem;
}

.feature-card h3 {
  color: #2c3e50;
  font-size: 1.3em;
  margin-bottom: 1rem;
}

.feature-card p {
  color: #7f8c8d;
  line-height: 1.6;
}

.groups-section {
  padding: 4rem 2rem;
  text-align: center;
  background: #f8f9fa;
  margin-top: 4rem;
}

.groups-section h2 {
  font-size: 2.5em;
  color: #2c3e50;
  margin-bottom: 3rem;
}

.explore-button {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  background: linear-gradient(135deg, #00B5AD 0%, #009c95 100%);
  color: white;
  padding: 1rem 2rem;
  border-radius: 8px;
  font-size: 1.2em;
  text-decoration: none;
  transition: all 0.3s ease;
  box-shadow: 0 4px 15px rgba(0, 181, 173, 0.2);
}

.explore-button:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(0, 181, 173, 0.3);
  color: white;
  text-decoration: none;
}

@media (max-width: 768px) {
  .hero-section {
    padding: 4rem 1rem;
  }

  .hero-section h1 {
    font-size: 2.5em;
  }

  .hero-subtitle {
    font-size: 1.2em;
  }

  .cta-buttons {
    flex-direction: column;
  }

  .features-grid {
    grid-template-columns: 1fr;
  }
}
</style> 