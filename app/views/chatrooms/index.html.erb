<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chat Activity</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css">
  <style>
    .chatbox {
      height: 450px;
      overflow-y: auto;
      padding: 10px;
    }
  </style>
</head>
<body>
  <div class="ui container" style="margin-top: 20px;">
    <!-- Header Section -->
    <header class="ui center aligned segment">
      <h4 class="ui center aligned medium icon header">
        <i class="circular users icon"></i>
        Chatrooms
      </h4>
    </header>

    <!-- Main Content -->
    <div class="ui stackable two column grid">
      <!-- Chatroom List -->
      <div class="twelve wide column">
        <div class="ui fluid raised card chatbox">
          <div class="content">
            <h4 class="ui header">Available Chatrooms</h4>
            <div class="ui relaxed divided list">
              <% @chatrooms.each do |chatroom| %>
                <div class="item">
                  <i class="large comments outline middle aligned icon"></i>
                  <div class="content">
                    <%= link_to chatroom.name, chatroom_path(chatroom), class: "header" %>
                  </div>
                </div>
              <% end %>
            <h4 class="ui header">Followers Activity</h4>
            <div class="ui small feed" id="messages">
              <%= render @messages %>
            </div>
          </div>
          <div class="extra content">
            <%= link_to "Create New Chatroom", new_chatroom_path, class: "ui button primary" %>
          </div>
        </div>
      </div>

      <!-- Navigation Menu -->
      <div class="four wide column">
        <div class="ui fluid raised card">
          <div class="content">
            <nav>
              <div class="ui inverted vertical menu">
                <%= link_to "Home", root_path, class: "active item" %>
                <%= link_to "Messages", chatrooms_path, class: "item" %>
                <a class="item">Friends</a>
              </div>
            </nav>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>

