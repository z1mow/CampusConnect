<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    .create-group-container {
      max-width: 600px;
      margin: 0 auto;
      padding: 2rem;
    }
    .custom-segment {
      background: rgba(255, 255, 255, 0.95) !important;
      border: none !important;
      box-shadow: 0 8px 32px rgba(31, 38, 135, 0.15) !important;
      backdrop-filter: blur(4px);
      border-radius: 15px !important;
      padding: 2rem !important;
    }
    .ui.form input {
      background: rgba(255, 255, 255, 0.9) !important;
      border-radius: 8px !important;
    }
    .custom-button {
      background: linear-gradient(135deg, #00B5AD 0%, #009c95 100%) !important;
      color: white !important;
      transition: transform 0.2s !important;
      margin-top: 1rem !important;
    }
    .custom-button:hover {
      transform: translateY(-2px);
    }
  </style>
</head>
<body>
  <div class="create-group-container">
    <h2 class="ui center aligned header" style="color: #2c3e50; font-size: 2.5em; margin-bottom: 1.5em;">
      Create a New Community Group
      <div class="sub header" style="color: #7f8c8d; margin-top: 0.5em; font-size: 0.5em;">
        Connect with Your Campus Community
      </div>
    </h2>

    <div class="ui segment custom-segment">
      <%= form_with model: @community_group, local: true, class: "ui large form" do |f| %>
        <div class="field">
          <%= f.label :name, "Group Name", style: "color: #2c3e50;" %>
          <div class="ui left icon input">
            <%= f.text_field :name, placeholder: "Enter your group name", class: "form-control" %>
            <i class="users icon"></i>
          </div>
        </div>

        <div class="field">
          <%= f.label :description, "Description", style: "color: #2c3e50;" %>
          <div class="ui left icon input">
            <%= f.text_area :description, placeholder: "Describe your group's purpose", rows: 4, style: "border-radius: 8px; resize: vertical;" %>
          </div>
        </div>

        <div class="field">
          <%= f.label :category, "Category", style: "color: #2c3e50;" %>
          <div class="ui selection dropdown" style="border-radius: 8px;">
            <%= f.select :category, 
              [
                ['Study Group', 'study'],
                ['Club', 'club'],
                ['Project Team', 'project'],
                ['Department', 'department'],
                ['Other', 'other']
              ],
              {}, 
              { class: "ui dropdown" } 
            %>
          </div>
        </div>

        <button type="submit" class="ui fluid large teal submit button custom-button">
          <i class="plus circle icon"></i>
          Create Community Group
        </button>
      <% end %>
    </div>
  </div>

  <script>
    $(document).ready(function() {
      $('.ui.dropdown').dropdown();
    });
  </script>
</body>
</html>
