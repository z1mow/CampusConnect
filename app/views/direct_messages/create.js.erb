var messagesContainer = document.getElementById('messages');
messagesContainer.insertAdjacentHTML('beforeend', '<%= j render partial: "direct_messages/message", locals: { message: @message } %>');
messagesContainer.scrollTop = messagesContainer.scrollHeight;

document.querySelector('#direct_message_content').value = ''; 