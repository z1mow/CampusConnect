<h1>Arama Sonuçları</h1>
<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
      <li>
        <%= link_to user.name, user_path(user) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>Sonuç bulunamadı.</p>
<% end %>