<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css">
  <style>
    body {
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      min-height: 100vh;
      margin: 0;
      padding: 20px 0;
    }
    .signup-container {
      max-width: 600px;
      margin: 0 auto;
      padding: 2rem;
    }
    .custom-segment {
      background: rgba(255, 255, 255, 0.95) !important;
      border: none !important;
      box-shadow: 0 8px 32px rgba(31, 38, 135, 0.15) !important;
      backdrop-filter: blur(4px);
      border-radius: 15px !important;
      padding: 2rem !important;
    }
    .ui.form input {
      background: rgba(255, 255, 255, 0.9) !important;
      border-radius: 8px !important;
    }
    .custom-button {
      background: linear-gradient(135deg, #00B5AD 0%, #009c95 100%) !important;
      color: white !important;
      transition: transform 0.2s !important;
      margin-top: 1rem !important;
    }
    .custom-button:hover {
      transform: translateY(-2px);
    }
    .ui.pointing.label {
      background: rgba(255, 255, 255, 0.9) !important;
      border: none !important;
      box-shadow: 0 2px 8px rgba(31, 38, 135, 0.1) !important;
      margin-top: 0.5rem !important;
    }
    .divider-text {
      text-align: center;
      margin: 2rem 0;
      color: #7f8c8d;
      font-weight: bold;
      position: relative;
    }
    .divider-text::before,
    .divider-text::after {
      content: "";
      position: absolute;
      top: 50%;
      width: 45%;
      height: 1px;
      background: rgba(127, 140, 141, 0.2);
    }
    .divider-text::before {
      left: 0;
    }
    .divider-text::after {
      right: 0;
    }
    .sign-in-button {
      color: #00B5AD !important;
      background: transparent !important;
      border: 2px solid #00B5AD !important;
      transition: all 0.3s ease !important;
    }
    .sign-in-button:hover {
      color: white !important;
      background: #00B5AD !important;
    }
  </style>
</head>
<body>
  <div class="signup-container">
    <h2 class="ui center aligned header" style="color: #2c3e50; font-size: 2.5em; margin-bottom: 1.5em;">
      Create Your Account
      <div class="sub header" style="color: #7f8c8d; margin-top: 0.5em; font-size: 0.5em;">
        Join CampusConnect Today
      </div>
    </h2>

    <div class="ui segment custom-segment">
      <%= form_with model: @user, url: users_path, local: true, class: "ui large form" do |f| %>
        <div class="field">
          <%= f.label :name, "Full Name", style: "color: #2c3e50;" %>
          <div class="ui left icon input">
            <%= f.text_field :name, placeholder: "Enter your full name" %>
            <i class="user icon"></i>
          </div>
        </div>

        <div class="field">
          <%= f.label :email, "Email Address", style: "color: #2c3e50;" %>
          <div class="ui left icon input">
            <%= f.email_field :email, placeholder: "Enter your email" %>
            <i class="envelope icon"></i>
          </div>
          <div class="ui pointing label">
            Only emails with <strong>@live.acibadem.edu.tr</strong> or <strong>@acibadem.edu.tr</strong> are allowed.
          </div>
        </div>

        <div class="field">
          <%= f.label :username, "Username", style: "color: #2c3e50;" %>
          <div class="ui left icon input">
            <%= f.text_field :username, placeholder: "Choose a username" %>
            <i class="user circle icon"></i>
          </div>
        </div>

        <div class="field">
          <%= f.label :password, "Password", style: "color: #2c3e50;" %>
          <div class="ui left icon input">
            <%= f.password_field :password, placeholder: "Create a password" %>
            <i class="lock icon"></i>
          </div>
        </div>

        <div class="field">
          <%= f.label :password_confirmation, "Confirm Password", style: "color: #2c3e50;" %>
          <div class="ui left icon input">
            <%= f.password_field :password_confirmation, placeholder: "Confirm your password" %>
            <i class="lock icon"></i>
          </div>
        </div>

        <button type="submit" class="ui fluid large teal submit button custom-button">
          <i class="signup icon"></i>
          Sign Up
        </button>
      <% end %>

      <div class="divider-text">
        ALREADY HAVE AN ACCOUNT?
      </div>

      <%= link_to login_path, class: "ui fluid large button sign-in-button" do %>
        Sign In
      <% end %>
    </div>
  </div>
</body>
</html>
